1. 解析後的問題:s2字串中是否包含s1字串?
2. 實作:使L、R作為window邊界，且window寬度為s1長度， 若ans數值==s1的字元數量則true，若否則持續跑至窗戶到達s2.size()，
    到達後若ans仍不等於s1字元數則false，ans依靠每次window移動後所遇見的新數值及拋棄的舊數值做變更，若移動後舊數值、
    新數值當前數量小於s1中舊數值的數量則ans--:ans++。
3. 成功，時間複雜度O(n)，但解答有更省空間、更迅速的寫法。