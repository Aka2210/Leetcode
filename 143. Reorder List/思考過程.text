一開始認為只需要設兩個point(L、R)在頭與尾的地方然後使L.next = R然後讓R連結到原本L的next就可以解決，但因為list必須由左到右，所以寫了一個翻轉函式，
    但return後發現，因為記憶體位址不是新設置的而是原本的，所以會導致原本由左到右的List變成由右到左，變成缺少了原本的List，所以此方法不可行，
    最後想出的方法是利用題目reorder的特性，先將每個值的記憶體位址儲存在vector中，並使得List的連接順序為1 -> n -> 2 -> n - 2...(1為vector第0格以此類推)，
    最後再標示出奇數、偶數的差別來解決此題。